# Karat extension examples

Each extension is one named folder containing `extension.json`. In Karat desktop, open the Extensions view and choose **Install from Folder**, then select `project-tools`.

Commands are declarative but can execute processes in the active workspace after a user clicks them. Inspect every manifest before installation.
